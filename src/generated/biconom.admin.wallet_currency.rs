// This file is @generated by prost-build.
#[derive(Clone, PartialEq, Eq, Hash, ::prost::Message)]
pub struct GiftRequest {
    /// Получатель подарка.
    #[prost(uint32, tag = "1")]
    pub distributor_id: u32,
    /// Тип кошелька получателя (например, "spot").
    /// Должен существовать, иначе — WALLET_TYPE_NOT_FOUND.
    #[prost(uint32, tag = "2")]
    pub wallet_type_id: u32,
    /// Валюта начисления. Пара (wallet_type_id, currency_id) должна быть
    /// разрешена в wallet_type_currency, иначе — WALLET_TYPE_CURRENCY_NOT_FOUND.
    #[prost(uint32, tag = "3")]
    pub currency_id: u32,
    /// Сумма подарка — decimal-строка (например "150.759").
    /// Лишние знаки после запятой сверх precision валюты отбрасываются.
    /// Результат после усечения должен быть строго > 0.
    #[prost(string, tag = "4")]
    pub amount: ::prost::alloc::string::String,
    /// Причина начисления — попадает в comment карточки подарка.
    #[prost(string, optional, tag = "5")]
    pub reason: ::core::option::Option<::prost::alloc::string::String>,
}
/// Generated server implementations.
pub mod wallet_currency_service_server {
    #![allow(
        unused_variables,
        dead_code,
        missing_docs,
        clippy::wildcard_imports,
        clippy::let_unit_value,
    )]
    use tonic::codegen::*;
    /// Generated trait containing gRPC methods that should be implemented for use with WalletCurrencyServiceServer.
    #[async_trait]
    pub trait WalletCurrencyService: std::marker::Send + std::marker::Sync + 'static {
        /// Подарить дистрибьютору средства в указанной валюте.
        /// Кошелёк получателя определяется связкой
        /// (distributor_id, wallet_type_id, currency_id).
        ///
        /// Возвращает созданную транзакцию в том же виде, что и элемент истории
        /// (biconom.client.transaction.TransactionService/History), но всегда
        /// с одной entry типа `gift` и без метаданных о дистрибьюторах/слотах/аккаунтах.
        async fn gift(
            &self,
            request: tonic::Request<super::GiftRequest>,
        ) -> std::result::Result<
            tonic::Response<super::super::super::types::transaction::Group>,
            tonic::Status,
        >;
    }
    /// WalletCurrencyService — админское начисление подарка на леджер
    /// дистрибьютора в указанной валюте.
    ///
    /// Механика — пополнение от платёжной сети cashdesk (тот же паттерн, что и
    /// `account_balance_refill`): cashdesk — Credit, кошелёк получателя — Debit.
    /// Кошелёк, wallet_currency-слот и леджер создаются при отсутствии.
    ///
    /// Доступ — только под Permission::ROOT.
    #[derive(Debug)]
    pub struct WalletCurrencyServiceServer<T> {
        inner: Arc<T>,
        accept_compression_encodings: EnabledCompressionEncodings,
        send_compression_encodings: EnabledCompressionEncodings,
        max_decoding_message_size: Option<usize>,
        max_encoding_message_size: Option<usize>,
    }
    impl<T> WalletCurrencyServiceServer<T> {
        pub fn new(inner: T) -> Self {
            Self::from_arc(Arc::new(inner))
        }
        pub fn from_arc(inner: Arc<T>) -> Self {
            Self {
                inner,
                accept_compression_encodings: Default::default(),
                send_compression_encodings: Default::default(),
                max_decoding_message_size: None,
                max_encoding_message_size: None,
            }
        }
        pub fn with_interceptor<F>(
            inner: T,
            interceptor: F,
        ) -> InterceptedService<Self, F>
        where
            F: tonic::service::Interceptor,
        {
            InterceptedService::new(Self::new(inner), interceptor)
        }
        /// Enable decompressing requests with the given encoding.
        #[must_use]
        pub fn accept_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.accept_compression_encodings.enable(encoding);
            self
        }
        /// Compress responses with the given encoding, if the client supports it.
        #[must_use]
        pub fn send_compressed(mut self, encoding: CompressionEncoding) -> Self {
            self.send_compression_encodings.enable(encoding);
            self
        }
        /// Limits the maximum size of a decoded message.
        ///
        /// Default: `4MB`
        #[must_use]
        pub fn max_decoding_message_size(mut self, limit: usize) -> Self {
            self.max_decoding_message_size = Some(limit);
            self
        }
        /// Limits the maximum size of an encoded message.
        ///
        /// Default: `usize::MAX`
        #[must_use]
        pub fn max_encoding_message_size(mut self, limit: usize) -> Self {
            self.max_encoding_message_size = Some(limit);
            self
        }
    }
    impl<T, B> tonic::codegen::Service<http::Request<B>>
    for WalletCurrencyServiceServer<T>
    where
        T: WalletCurrencyService,
        B: Body + std::marker::Send + 'static,
        B::Error: Into<StdError> + std::marker::Send + 'static,
    {
        type Response = http::Response<tonic::body::Body>;
        type Error = std::convert::Infallible;
        type Future = BoxFuture<Self::Response, Self::Error>;
        fn poll_ready(
            &mut self,
            _cx: &mut Context<'_>,
        ) -> Poll<std::result::Result<(), Self::Error>> {
            Poll::Ready(Ok(()))
        }
        fn call(&mut self, req: http::Request<B>) -> Self::Future {
            match req.uri().path() {
                "/biconom.admin.wallet_currency.WalletCurrencyService/Gift" => {
                    #[allow(non_camel_case_types)]
                    struct GiftSvc<T: WalletCurrencyService>(pub Arc<T>);
                    impl<
                        T: WalletCurrencyService,
                    > tonic::server::UnaryService<super::GiftRequest> for GiftSvc<T> {
                        type Response = super::super::super::types::transaction::Group;
                        type Future = BoxFuture<
                            tonic::Response<Self::Response>,
                            tonic::Status,
                        >;
                        fn call(
                            &mut self,
                            request: tonic::Request<super::GiftRequest>,
                        ) -> Self::Future {
                            let inner = Arc::clone(&self.0);
                            let fut = async move {
                                <T as WalletCurrencyService>::gift(&inner, request).await
                            };
                            Box::pin(fut)
                        }
                    }
                    let accept_compression_encodings = self.accept_compression_encodings;
                    let send_compression_encodings = self.send_compression_encodings;
                    let max_decoding_message_size = self.max_decoding_message_size;
                    let max_encoding_message_size = self.max_encoding_message_size;
                    let inner = self.inner.clone();
                    let fut = async move {
                        let method = GiftSvc(inner);
                        let codec = tonic_prost::ProstCodec::default();
                        let mut grpc = tonic::server::Grpc::new(codec)
                            .apply_compression_config(
                                accept_compression_encodings,
                                send_compression_encodings,
                            )
                            .apply_max_message_size_config(
                                max_decoding_message_size,
                                max_encoding_message_size,
                            );
                        let res = grpc.unary(method, req).await;
                        Ok(res)
                    };
                    Box::pin(fut)
                }
                _ => {
                    Box::pin(async move {
                        let mut response = http::Response::new(
                            tonic::body::Body::default(),
                        );
                        let headers = response.headers_mut();
                        headers
                            .insert(
                                tonic::Status::GRPC_STATUS,
                                (tonic::Code::Unimplemented as i32).into(),
                            );
                        headers
                            .insert(
                                http::header::CONTENT_TYPE,
                                tonic::metadata::GRPC_CONTENT_TYPE,
                            );
                        Ok(response)
                    })
                }
            }
        }
    }
    impl<T> Clone for WalletCurrencyServiceServer<T> {
        fn clone(&self) -> Self {
            let inner = self.inner.clone();
            Self {
                inner,
                accept_compression_encodings: self.accept_compression_encodings,
                send_compression_encodings: self.send_compression_encodings,
                max_decoding_message_size: self.max_decoding_message_size,
                max_encoding_message_size: self.max_encoding_message_size,
            }
        }
    }
    /// Generated gRPC service name
    pub const SERVICE_NAME: &str = "biconom.admin.wallet_currency.WalletCurrencyService";
    impl<T> tonic::server::NamedService for WalletCurrencyServiceServer<T> {
        const NAME: &'static str = SERVICE_NAME;
    }
}
